(function () {		var form = document.getElementById('person-form');	var submit_form_btn = document.getElementById('submit');			submit_form_btn.addEventListener('click', function () {		if (form.checkValidity()) {			var name = document.getElementById('name').value;			var food = document.getElementById('food').value;			const person = new Person(name, food);			alert(person.message);		} else {			form.querySelector('input[type="submit"]').click();		}	}, false);		class Person {		constructor(name, food) {			this.name = name;			this.food = food;		}		// Getter		get showAlert() {			alert(this.message)		}		get message() {			return this.name + " is eating: " + this.food;		}	}	})();